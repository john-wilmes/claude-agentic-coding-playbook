# Deprecated Profile: research

This profile is no longer installed or maintained.

The `dev` and `research` profiles were merged into `profiles/combined/`, which is the only profile installed by `install.sh`. See `profiles/combined/` for the current CLAUDE.md, skills, and settings.
