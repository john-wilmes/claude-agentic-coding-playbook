# RTK Token Optimizer

rtk is installed and active. It automatically compresses Bash tool output to save tokens.

- `rtk gain` — show token savings statistics
- `rtk discover` — find missed optimization opportunities in past sessions
- rtk hooks intercept Bash commands transparently; no action needed from the agent
